# Eye Tracking Module (Python)
# Bind address for the WebSocket server (0.0.0.0 exposes it to the network)
GAZE_WS_HOST=0.0.0.0
# Port used to stream gaze data to the render module
GAZE_WS_PORT=8765

# Render Module (Vite / Browser)
# Override the WebSocket endpoint the browser should connect to
VITE_GAZE_WS_URL=ws://localhost:8765
VITE_GAZE_WS_HOST=localhost
VITE_GAZE_WS_PORT=8765

